# Arian Backend API Configuration
ARIAND_URL=grpc://localhost:50051
USER_ID=your-user-uuid-here
API_KEY=your-api-key-here

# Mailpit Configuration
MAILPIT_URL=http://localhost:8025
MAILPIT_USERNAME=your-mailpit-username
MAILPIT_PASSWORD=your-mailpit-password

# Webhook Mode Configuration (optional)
# If both are set, app runs as webhook server
# If both are empty, app runs in one-shot mode
PARSER_WEBHOOK_PATH=/webhook/parse
PARSER_LISTEN_ADDR=:8080